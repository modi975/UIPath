This workflow extracts the economic loss zone indicator for each address from Addresses.xlsx document.
To be able to execute this workflow you must have Flash and Excel installed.
The workflow goes through every row in the excel file, reads the address, introduces it in the flash application,
extracts the indicator loss value and writes it back to the excel file.